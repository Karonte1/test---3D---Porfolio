// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://tu-usuario.github.io',
  base: '/tu-repositorio/', // ¡Ojo con las barras '/' al principio y al final!
});